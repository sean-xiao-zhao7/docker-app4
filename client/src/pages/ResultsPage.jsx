import { Link } from "react-router-dom";

const ResultsPage = (props) => {
    return (
        <>
            <h1>Fibonacci results page</h1>
            <Link to="/">Back home</Link>
        </>
    );
};

export default ResultsPage;
